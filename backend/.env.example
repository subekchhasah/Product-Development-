PORT=5001
JWT_SECRET=your_jwt_secret_here

# Database Configuration (PostgreSQL by default, will fallback to SQLite if DB_URL is not set)
# DATABASE_URL=postgres://username:password@localhost:5432/ai_solutions

# Default Admin User
ADMIN_DEFAULT_USER=admin
ADMIN_DEFAULT_PASSWORD=admin123

# Optional AI Integration API Keys
OPENAI_API_KEY=
GEMINI_API_KEY=
